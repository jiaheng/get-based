# Oura — brand assets

## Status
**OFFICIAL (sourced from public site, currentColor).**

## Files
- `wordmark.svg` — Oura wordmark using `currentColor` so a single file works on light + dark themes
- `mark-mono.svg` — fallback glyph

## Source
- Extracted from inline SVG on https://ouraring.com/ (header logomark, public). Oura embeds their logo inline — no separate downloadable file located.

## Constraints
- Oura's brand guideline is monochrome — no fixed brand colour. We render the wordmark on our generic accent pill (no `brandColor` in registry).
- Brand kit gated behind cloud.ouraring.com developer login. Pull the official Sign-in-with-Oura button when accessing the dev console next.
- Logo is trademark-protected; use the official SVG, don't redraw.
